// Deklaration und Initialisierung einer Konstanten mit `const`
const myConst = "Initialer Wert";

// Versuch, den Wert der Konstanten zu ändern
myConst = "Neuer Wert"; // TypeError: Assignment to constant variable