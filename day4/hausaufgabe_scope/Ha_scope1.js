// Schleife mit let deklariert
for (let i = 0; i < 5; i++) {
    console.log("Schleifenvariable i innerhalb der Schleife:", i);
}

// Zugriff auf die Schleifenvariable außerhalb der Schleife
console.log("Schleifenvariable i außerhalb der Schleife:",i); // Referenzfehler: i is not defined